# TODO: Fix Lint Errors in Next.js Project

- [ ] Fix error in src/app/proyecto/ciudades/[nombre]/page.tsx: Replace `<a>` with `<Link>`
- [ ] Fix warning in src/components/CityCard.tsx: Replace `<img>` with `<Image>` from next/image
- [ ] Fix warning in src/components/ImageWrapper.tsx: Replace `<img>` with `<Image>` from next/image
- [ ] Fix warning in src/components/StudentCard.tsx: Replace `<img>` with `<Image>` from next/image
- [ ] Run lint again to verify all errors are fixed
